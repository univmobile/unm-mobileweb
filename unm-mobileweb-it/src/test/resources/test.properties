AppPath=${AppPath}
ios.platformVersion.shouldBe=${ios.platformVersion.shouldBe}

mysql.url=${mysql.url}
mysql.username=${mysql.username}
mysql.password.ref=${mysql.password.ref}

clients_plist_source=${clients_plist_source}
clients_plist_target=${clients_plist_target}
